import { View, Text } from "react-native";

/**
 * Renders the loading screen
 * @returns {JSX.Element}
 */
function Loading() {
    return (
        <View>
            <Text>Loading...</Text>
        </View>
    );
}

export default Loading;